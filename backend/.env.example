# Variables de entorno para el backend de Finanzas Pro
# Copia este archivo a .env y rellena los valores para tu entorno de desarrollo.

# URL de conexión a la base de datos PostgreSQL.
# Formato: postgresql://<USUARIO>:<CONTRASEÑA>@<HOST>:<PUERTO>/<BASE_DE_DATOS>
# En el entorno de Docker Compose, el HOST es el nombre del servicio de la base de datos.
DATABASE_URL="postgresql://<USUARIO>:<CONTRASEÑA>@<HOST>:<PUERTO>/<BASE_DE_DATOS>"
# Otras variables que tu aplicación pueda necesitar (ej: secretos para JWT, etc.)
JWT_SECRET="tu_secreto_aqui"
#Puerto de la API
PORT="tu_puerto_aqui"
