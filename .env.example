# =============================================================================
# Finanzas Pro - Docker Compose Environment Variables
# =============================================================================
# Copia este archivo a .env y configura los valores.
# =============================================================================

# --- Base de Datos (Común para todos) ---
POSTGRES_USER=finanzas
POSTGRES_PASSWORD=cambia_esta_password
POSTGRES_DB=finanzas_pro

# --- Cloudflare Tunnel (SOLO para opción Cloudflare) ---
# Si usas docker-compose.selfhosted.yml, puedes ignorar o borrar esta línea.
#
# Obtén tu token en: https://one.dash.cloudflare.com/
CLOUDFLARE_TUNNEL_TOKEN=tu_token_aqui
