# =============================================================================
# Finanzas Pro - Docker Compose Environment Variables
# =============================================================================
# Copia este archivo a .env y configura los valores.
# =============================================================================

# --- Base de Datos (Común para todos) ---
POSTGRES_USER=finanzas
POSTGRES_PASSWORD=cambia_esta_password
POSTGRES_DB=finanzas_pro

# --- Cloudflare Tunnel (SOLO para opción Cloudflare) ---
# Si usas docker-compose.selfhosted.yml, puedes ignorar o borrar esta línea.
#
# Obtén tu token en: https://one.dash.cloudflare.com/
CLOUDFLARE_TUNNEL_TOKEN=tu_token_aqui

# =============================================================================
# Configuración de Seguridad (Backend)
# =============================================================================

# --- CORS: Orígenes permitidos ---
# Lista separada por comas de dominios que pueden acceder a la API.
# Déjalo vacío o usa '*' para desarrollo (permite todos los orígenes).
# Ejemplo producción: https://finanzas.tudominio.com,https://tudominio.com
ALLOWED_ORIGINS=https://tu-dominio.com

# --- Rate Limiting ---
# Protección contra ataques de fuerza bruta.
# Establece en 'false' SOLO para desarrollo/debugging.
RATE_LIMIT_ENABLED=true

# --- Control de Registro ---
# Establece en 'false' para deshabilitar el registro de nuevos usuarios.
# Útil cuando ya tienes todos los usuarios que necesitas.
REGISTRATION_ENABLED=false

# =============================================================================
# Configuración de Backups (Opcional)
# =============================================================================

# --- Notificaciones por Telegram ---
# Recibe alertas cuando se complete o falle un backup.
# 1. Crea un bot con @BotFather en Telegram y copia el token
# 2. Obtén tu chat_id enviando un mensaje a @userinfobot
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=tu_bot_token_aqui
TELEGRAM_CHAT_ID=tu_chat_id_aqui

# --- Configuración de rclone ---
# RCLONE_REMOTE=gdrive
# RCLONE_PATH=finanzas-pro-backups
