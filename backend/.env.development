# =============================================================================
# backend/.env.development - Variables para DESARROLLO LOCAL
# =============================================================================
# Copia este archivo: cp .env.development .env
# Las credenciales deben coincidir con docker-compose.dev.yml (raíz)
# =============================================================================

# --- Base de Datos PostgreSQL ---
# Host: localhost (desarrollo local) | db (Docker)
DATABASE_URL=postgresql://finanzas_dev:devfinanzas@localhost:5432/finanzas_dev

# --- Servidor ---
PORT=4000
NODE_ENV=development

# --- Autenticación JWT ---
# NOTA: Este secreto es SOLO para desarrollo. Producción debe usar uno diferente.
JWT_SECRET=dev-jwt-secret-local-only-do-not-use-in-production

# --- URL de la Aplicación (para emails) ---
APP_URL=http://localhost:5173

# --- CORS ---
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# --- Rate Limiting (deshabilitado en dev) ---
RATE_LIMIT_ENABLED=false

# --- Registro de usuarios (abierto en dev) ---
REGISTRATION_ENABLED=true

# --- Cronjobs (deshabilitados en dev) ---
ENABLE_CRON_JOBS=false
