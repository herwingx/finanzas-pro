# =============================================================================
# .env.example - Variables de Entorno (Raíz)
# =============================================================================
# DESARROLLO: Solo copia este archivo → cp .env.example .env
# PRODUCCIÓN: Consulta README.md → Sección "Variables de Entorno"
# =============================================================================

# -----------------------------------------------------------------------------
# Base de Datos PostgreSQL
# -----------------------------------------------------------------------------
POSTGRES_USER=finanzas
POSTGRES_PASSWORD=devfinanzas                    # PROD: Usa password seguro
POSTGRES_DB=finanzas_pro

# -----------------------------------------------------------------------------
# Cloudflare Tunnel (solo producción)
# -----------------------------------------------------------------------------
# Obtén tu token en: https://one.dash.cloudflare.com/
# Zero Trust → Networks → Tunnels → Create a tunnel
CLOUDFLARE_TUNNEL_TOKEN=

# -----------------------------------------------------------------------------
# Seguridad
# -----------------------------------------------------------------------------
# Orígenes permitidos para CORS (separados por coma)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000    # PROD: https://tu-dominio.com

# Protección contra fuerza bruta
RATE_LIMIT_ENABLED=false                         # PROD: true

# Control de registro de nuevos usuarios
REGISTRATION_ENABLED=true                        # PROD: false (después de crear usuarios)

# -----------------------------------------------------------------------------
# Telegram Backups (opcional)
# -----------------------------------------------------------------------------
# Recibe notificaciones cuando se complete o falle un backup
# 1. Crea un bot con @BotFather y copia el token
# 2. Obtén tu chat_id enviando un mensaje a @userinfobot
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
