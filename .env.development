# =============================================================================
# .env.development - Variables para DESARROLLO LOCAL (Docker Compose)
# =============================================================================
# Copia este archivo: cp .env.development .env
# Luego: docker compose -f docker-compose.dev.yml up -d
# =============================================================================

# --- Base de Datos (para docker-compose.dev.yml) ---
POSTGRES_USER=finanzas_dev
POSTGRES_PASSWORD=devfinanzas
POSTGRES_DB=finanzas_dev
