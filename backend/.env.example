# =============================================================================
# backend/.env.example - Variables para PRODUCCIÓN
# =============================================================================
# Copia este archivo: cp .env.example .env
# Configura los valores para tu entorno de producción.
# =============================================================================

# --- Base de Datos PostgreSQL ---
# Host: db (nombre del servicio en docker-compose.yml)
DATABASE_URL=postgresql://finanzas:CAMBIA_ESTA_PASSWORD@db:5432/finanzas_pro

# --- Servidor ---
PORT=4000
NODE_ENV=production

# --- Autenticación JWT ---
# Genera un string aleatorio: openssl rand -hex 32
JWT_SECRET=GENERA_UN_STRING_ALEATORIO_LARGO_AQUI

# --- URL de la Aplicación (para emails) ---
APP_URL=https://tu-dominio.com

# --- CORS ---
ALLOWED_ORIGINS=https://tu-dominio.com

# --- Rate Limiting (habilitado en prod) ---
RATE_LIMIT_ENABLED=true

# --- Registro de usuarios (cerrado en prod) ---
REGISTRATION_ENABLED=false

# --- Cronjobs (habilitados en prod) ---
ENABLE_CRON_JOBS=true

# --- SMTP (opcional, para recuperación de contraseña) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=tu-app-password
# SMTP_FROM=Finanzas Pro <noreply@tu-dominio.com>
